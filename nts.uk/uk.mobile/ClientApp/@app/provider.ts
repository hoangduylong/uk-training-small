import Vuex from 'vuex';
import VueRouter, { Route } from 'vue-router';
import Vue, { ComponentOptions, VNode, VNodeDirective, DirectiveFunction, VueConstructor, CreateElement } from 'vue';

import * as _ from 'lodash';
import * as moment from 'moment';
import { DirectiveBinding } from 'vue/types/options';

export { _, moment, Vue, Vuex, Route, VueRouter, ComponentOptions, DirectiveBinding, VNodeDirective, VNode, VueConstructor, CreateElement, DirectiveFunction };